%h4 #{t 'comments.lb_comments'}
.hr_margin
  %hr
-if @comments.empty?
  .alert.alert-info
    #{t 'comments.lb_no_comments'}
%ul.comments
  - @comments.each do |cmnt|
    -cache cmnt do
      = render 'comments/comment', :cmnt => cmnt
= paginate @comments, :theme => 'twitter-bootstrap-3'
=# will_paginate @comments, renderer: BootstrapPagination::Rails