%p
  #{t 'devise.mailer.lb_hello'} #{@resource.email}!
%p #{t 'devise.mailer.lb_your_account_has_been_locked'}
%p #{t 'devise.mailer.lb_click_the_link_below_to_unlock_your_account'}
%p= link_to "#{t 'devise.mailer.l_unlock_my_account'}", unlock_url(@resource, unlock_token: @token)
